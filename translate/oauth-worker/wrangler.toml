name = "chuck-oauth"
main = "index.js"
compatibility_date = "2024-12-01"

# Secrets (set via CLI, never commit these):
#   npx wrangler secret put GITHUB_OAUTH_CLIENT_SECRET
#
# Non-secret vars:
[vars]
GITHUB_OAUTH_CLIENT_ID = "Ov23lisWAFMY0ViOn5vf"
ALLOWED_ORIGINS = "https://chuckleberry-finn.github.io"
